import { EvenLengthDirective } from './even-length.directive';

describe('EvenLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenLengthDirective();
    expect(directive).toBeTruthy();
  });
});
